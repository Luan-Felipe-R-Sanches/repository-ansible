---
- name: Testando variaveis
  hosts: localhost
  vars_files: /etc/ansible/vars_files.yaml
  tasks:
    - name: DNF | Install packges
      ansible.builtin.dnf:
        name: "{{ packages }}"
        state: latest
     
...

